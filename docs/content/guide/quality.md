---
title: 品質チェック
order: 7
description: yuzu fmt / lint / check で文書の品質を CI で保つ
aliases: ["guide/lint/"]
lintDisable:
  - fullwidth-alphanumeric
  - halfwidth-kana
---

# 品質チェック

コードにフォーマッタと linter があるように、yuzu は設計書にも同じ道具を
用意しています。3 コマンドとも終了コードは **0 = 違反なし / 1 = 違反あり /
2 = 実行エラー**の規約で、そのまま CI に組み込めます。

## yuzu fmt — 決定的整形

```bash
yuzu fmt          # 正規形へ整形して書き込む
yuzu fmt --check  # 差分があれば終了コード 1（CI 用）
yuzu fmt --diff   # 書き換えず、整形前後の差分を表示（--check を含意）
```

AST 経由の決定的な整形で、見出しの ATX 化・箇条書きの `-` 統一などを
行います。次の不変条件を守ります:

- **frontmatter は生テキストをバイト温存**（整形対象は本文だけ）
- **冪等**（2 回かけても結果は同じ）
- 差分のないファイルには書き込まない（mtime 温存）

`--diff` の出力は**標準出力が diff 本体だけ**（件数の集計は標準エラー）なので、
そのままパッチとして使えます:

```bash
yuzu fmt --diff > format.patch && patch -p1 < format.patch
```

> [!NOTE]
> 改行コードが CRLF の原稿は**全行が差分**になります（整形結果は常に LF のため）。
> `--check` でも同じ判定ですが、`--diff` で初めて目に見えます。

## yuzu lint — 文書規約と表記ゆれ

```bash
yuzu lint        # 違反を行番号付きで報告
yuzu lint --fix  # 表記ゆれ系の変換候補を自動適用
```

文書規約（h1 の重複・見出しレベルの飛び・frontmatter の未知キー・
コードブロックの表示メタ検査 = `showLineNumbers` のタイポや範囲外の
行ハイライトの警告）に加えて、日本語の表記ゆれを検出します。

### 用語統一の辞書（lint.terms）

「正しい表記 → ゆれ表記」の辞書を書くと、本文・見出しのゆれを
行番号・列番号付きで報告します（コードブロック・URL は対象外）:

```toml
[lint.terms]
"サーバ" = ["サーバー"]
"ユーザ" = ["ユーザー"]
```

この辞書は[検索のクエリ拡張](search.md)にも使われます。

### 組み込みルール

辞書がなくても、機械的なゆれは既定で検出します:

| ルール | 検出対象 | 例 |
| --- | --- | --- |
| `fullwidth-alphanumeric` | 全角英数字 | Ｗｅｂ１２３ → `Web123` を提案 |
| `halfwidth-kana` | 半角カナ | ﾃﾞｰﾀ → `データ` を提案 |
| `katakana-choon` | 長音符ゆれの混在 | プロジェクト内の多数派へ寄せる警告 |

`--fix` は変換候補をまとめて適用します（コードブロックは触らず、冪等で、
差分のないファイルには書き込みません。長音符ゆれは多数派へ統一し、
同数のときは正解を決められないため報告のみです）。

ここに挙げた 3 つ以外にも規約系のルールがあります。全ルールの一覧は
[診断ルール](../reference/rules.md)を参照してください。

意図的な例外は、狭い順に 3 つの粒度で通せます — 1 行だけなら行コメント、
ページ全体なら frontmatter の `lintDisable`、プロジェクト方針として
切るなら `lint.rules`。以下で順に説明します。

### ページ単位の抑制（lintDisable）

「表記ゆれそのものを解説するページ」「あえて少数派の表記で書くページ」のような
意図的な例外は、frontmatter の `lintDisable` で**そのページに限り**ルールを
抑制できます。`yuzu check` を CI に乗せたまま例外ページを共存させる仕組みです:

```yaml
---
title: 表記ゆれの解説
lintDisable:
  - term-variant
  - katakana-choon
---
```

- 抑制できるのは warning のルールだけです。error（リンク切れ等）は生成物が
  壊れるのを防ぐルールなので抑制できず、指定すると `invalid-lint-suppression`
  の警告になります（未知のルール名も同様）
- 書いたのにそのページで一度も発火しなかった抑制は `unused-lint-suppression`
  の警告になります。原稿を直したら抑制も削除してください（放置すると
  「効いているつもり」の指定が溜まります）
- 抑制された出現は `yuzu lint --fix` でも書き換えません。集計行には
  「（抑制 N 件）」と件数が出ます

なお、このページ自身も上の「組み込みルール」の表で例を生のまま載せるために、
frontmatter の `lintDisable` を使っています（原文 Markdown の frontmatter が
そのまま実例です）。

### 行単位の抑制（yuzu-lint-disable-next-line）

ページ全体ではなく 1 箇所だけ例外を通したいときは、HTML コメントで
**次の内容行に限り**ルールを抑制できます:

```md
<!-- yuzu-lint-disable-next-line term-variant -->
この行のゆれ表記だけ報告されません。次の行からは通常どおりです。
```

- 対象は「コメントの後、空行を飛ばした次の内容行」です。コメントと対象行は
  密着させて書くのが正規形で、`yuzu fmt` もこの形へ整えます
- ルール名は空白区切りで複数指定できます。抑制できるのは warning のルールだけで、
  未知・抑制不可のルール名は `invalid-lint-suppression` の警告になります
- コメントは同じ行で `-->` を閉じてください。閉じ忘れると以降の本文が丸ごと
  HTML コメントに飲み込まれるため、`invalid-lint-suppression` が警告します
- 次の内容行で一度も発火しなかった抑制は `unused-lint-suppression` の警告に
  なります。原稿を直したらコメントも削除してください
- コメントは出力 HTML・配信ページの .md・llms-full.txt にそのまま残ります
  （ブラウザ表示には出ません）。検索の索引には入りません

### プロジェクト全体の無効化（lint.rules）

方針と合わないルールは `yuzu.toml` で丸ごと切れます。既存プロジェクトへの
段階導入（まず気になるルールを off で入れて、直しながら少しずつ有効化する）の
入口です:

```toml
[lint.rules]
katakana-choon = false
```

- 対象は warning のルールだけです（error は生成物が壊れるのを防ぐ正なので
  切れません）。warning でも `config-*`（`yuzu.toml` 自体の検査）と抑制機構自身
  （`invalid-lint-suppression` / `unused-lint-suppression`）は切れません。
  書かない ID は有効のままです
- ルール ID のタイポや旧形式のキーは、無効化できる ID の一覧付きの設定エラーに
  なります（黙って効かないまま進むことはありません）
- 無効化で落とした件数は集計行に「（無効化 N 件）」と出ます。件数は
  ページ・行単位の抑制と突き合わせる前に数えるため、再有効化したときに
  そのまま表に出る件数とは限りません（抑制に吸収された分は「抑制 N 件」側に出ます）
- 無効化中のルールへのページ・行単位の抑制は `unused-lint-suppression` に
  なりません（再有効化すると抑制がそのまま生き返ります）

### ディレクトリ階層の制限（`lint.max_directory_depth`）

`content` 配下のディレクトリの深さを制限できます（直下 = 0）。サイトの
構成が意図せず深くなるのを防ぎます。

## yuzu check — 統合チェック

```bash
yuzu check
```

lint のすべてに加えて、**内部リンク・アンカー切れ**（ページ間リンク・
`#見出し` 参照・画像などの添付ファイル参照）を行番号付きで報告する
統合コマンドです。CI ではこれ 1 つを回せば十分です。

> [!NOTE]
> fmt / lint / check は `draft: true` の下書きページも対象です
> （公開前の文書も同じ規約で保てます）。
> 
> このサイト自身も、デプロイ前に `yuzu check` を通しています
> （[配信とデプロイ](deploy.md)参照）。

## CI に組み込む

終了コードだけでも CI のゲートになりますが、`--format github` を使うと
GitHub Actions の注釈としてプルリクエストの diff 行に直接出せます。
「ジョブが赤い」だけでログを開かずに済みます。

```yaml
- name: docs check
  run: |
    cd docs
    yuzu check --format github
```

パスは `GITHUB_WORKSPACE` からの相対に自動で付け替わるので、上のように
サブディレクトリへ移動してから実行しても正しいファイルに紐づきます。

機械処理したいときは `--format json` を使います（標準出力へ単一の JSON
オブジェクトだけを出します）。出力の詳細は
[CLI コマンド](../reference/cli.md)の「診断の出力形式」を参照してください。
